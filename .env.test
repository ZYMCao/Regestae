# 测试环境配置（用于 build:test 打包给测试服务器）
# 关键点：build --mode test 时 Vue CLI 默认 NODE_ENV 会变成 development，
# 必须显式声明 NODE_ENV=production 才能走生产构建流程（压缩、去警告等）
NODE_ENV=production

VUE_APP_TITLE='电子档案管理系统(测试)'

# 测试环境后端地址：一般走 nginx 反向代理，/test-api 由运维转发到测试后端
VUE_APP_BASE_API='/test-api'
