# 生产环境配置
# （NODE_ENV 由 Vite 按命令自动决定：dev 为 development，build 为 production，无需声明）

VITE_TITLE='电子档案管理系统'

# 生产环境后端地址：同样由 nginx 反向代理转发
VITE_BASE_API='/prod-api'

# 静态 DEMO 开关：true 时登录/获取用户信息/退出均不走后端接口（本地模拟数据）
# 需要联调真实后端时改为 false 或删除此行
VITE_USE_MOCK=true
